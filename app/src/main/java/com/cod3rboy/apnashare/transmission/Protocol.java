package com.cod3rboy.apnashare.transmission;

public class Protocol {
    /**
     * VERSION constant represent the version of transmission protocol.
     * When transmission protocol is changed or modify, VERSION must also be incremented here.
     * This helps in the handshaking of client and server and it also help in reporting of
     * protocol version mismatch.
     */
    public static final int VERSION = 1;
}
